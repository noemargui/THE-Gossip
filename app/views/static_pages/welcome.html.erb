<html>
  <head>
    <title>Welcome</title>   
  </head>

  <body>
    <h1>BIENVENUE <%= params[:id] %>! Ici c’est notre super site de potins, il est chouette, non ?</h1>
    <% @gossips.each do |gossip| %>
      <p>
        <%= gossip.user.first_name %><br/>
        <%= gossip.title %><br/>
        <a href="/static_pages/gossipdetails/<%= gossip.id %>">Voir plus</a> 
      </p>
    <% end %>
  </body>
</html>